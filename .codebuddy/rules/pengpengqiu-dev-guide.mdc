---
description: 碰碰球（Brick Breaker / Ball Bounce）微信小游戏的完整开发指南。包含游戏玩法机制、UI布局、核心系统、数据结构、关卡设计、特效系统等全面描述。在开发碰碰球游戏功能时请加载此 Skill。
alwaysApply: false
enabled: true
updatedAt: 2026-05-17T04:41:10.633Z
provider: 
---

# 碰碰球（PengPengQiu）微信小游戏开发指南

## 一、游戏概述

碰碰球是一款经典的弹球消砖块类微信小游戏。玩家从屏幕底部发射大量小球，小球向上运动碰撞砖块，每次碰撞使砖块数值减1，砖块数值归零后被消除。所有球落回底部后，砖块整体下移一行，新砖块从顶部生成。当任意砖块到达底部发射线时游戏结束。

**平台**: 微信小游戏（wx.createCanvas）
**方向**: 竖屏（portrait）
**渲染**: Canvas 2D
**架构**: 单例 DataBus 全局状态管理 + 对象池 + 帧循环

---

## 二、UI 布局（从上到下）

### 2.1 顶部信息栏（Top HUD）
- **左上角**: 瞄准辅助线开关按钮（圆形图标，带发光边框）
- **中间上方**: 关卡显示 `STAGE-{n}`，如 `STAGE-31`
- **中间下方**: 星级进度条（两颗星之间有进度条，根据消除砖块进度填充）
- **右上角**: 暂停按钮（圆形图标，双竖线 `||`）
- **进度条下方**: `Line {当前行数}` 和 当前分数显示

### 2.2 游戏主区域（Game Area）
- **边框**: 霓虹蓝色发光边框包围整个游戏区域
- **网格系统**: 7列 × N行的砖块网格（可见区域约10行）
- **砖块区域**: 砖块从顶部逐行向下排列
- **空白区域**: 砖块下方为球的运动空间
- **发射点**: 底部中央偏下，显示小球图标和球数 `x {球数量}`，如 `x 57`

### 2.3 底部操作栏（Bottom HUD）
- **左侧**: 闪电技能按钮（红色圆形，闪电图标，`x {次数}`），如 `x 4`
- **中间**: 能量/蓄力进度条（长条形，中间有球形指示器）
- **右侧**: 额外生命/多球技能按钮（红色圆形，多球图标，`x {次数}`），如 `x 5`

---

## 三、核心游戏机制

### 3.1 发射系统（Launch System）
- **发射位置**: 屏幕底部游戏区域内，所有球从同一点发射
- **瞄准方式**: 玩家触摸屏幕并滑动来调整发射角度
- **辅助瞄准线**: 从发射点到目标方向的虚线轨迹（可通过左上角按钮开关）
- **发射行为**: 
  - 松手后，球以固定间隔依次发射（不是同时发射）
  - 每个球发射间隔约 50-80ms
  - 所有球以相同角度和速度发射
  - 球速恒定，不受重力影响
- **发射角度限制**: 不能水平或向下发射，角度范围约 10°~170°（相对水平线）
- **球数量**: 显示在发射点上方，如 `x 57`

### 3.2 球物理系统（Ball Physics）
- **运动方式**: 匀速直线运动
- **碰撞反弹**: 
  - 碰到左右墙壁：水平速度取反
  - 碰到顶部墙壁：垂直速度取反
  - 碰到砖块：根据碰撞面（上/下/左/右）反弹
  - 碰到底部边界：球停止，等待回收
- **球的回收**: 
  - 球落到底部后停在底边
  - 所有球落回后，以第一个落回的球的位置作为下一轮发射点
  - 下一轮发射点会显示在新的位置
- **球的视觉**: 白色/浅色小圆球，带轻微发光效果
- **球的轨迹**: 运动中留下短暂的点状轨迹（如图中紫色虚线点）

### 3.3 砖块系统（Brick System）

#### 砖块类型
1. **普通砖块（Number Brick）**
   - 显示数值（如 44、46、47、48）
   - 每次被球碰撞，数值 -1
   - 数值为 0 时消除，播放消除特效
   - 颜色根据数值范围变化（低→红/暖色，高→紫/冷色）
   - 带霓虹发光边框效果

2. **加球砖块（Ball Pickup）**
   - 圆形，带特殊标记（如圆圈中带圆点的图标 ⊙）
   - 球碰到后拾取，下一轮球数量 +1
   - 不参与数值碰撞，碰到即消失
   - 发光白/紫色

3. **三角砖块（Triangle Brick）**
   - 三角形形状（如图中左下角区域可见）
   - 斜面反弹：球碰到斜面按照斜面法线反弹
   - 同样有数值，碰撞后数值 -1
   - 为关卡增加变化性

#### 砖块数值规则
- 新砖块的数值 = 当前关卡数（Stage Number）× 系数
- 例如 Stage 31 时，新砖块数值为 47、48 等
- 随着关卡推进，砖块数值线性增长
- 同一行砖块数值相同或接近

#### 砖块颜色方案（霓虹风格）
- 数值较高的新砖块: 紫色/蓝紫色
- 数值中等的砖块: 红色/橙红色
- 数值较低即将消除的砖块: 深红色
- 所有砖块带发光边框（glow effect）

#### 砖块下移规则
- 每轮（所有球落回后）：所有现存砖块向下移动一行
- 顶部新生成一行砖块（随机分布，不一定填满每格）
- 同时随机生成 1-3 个加球道具

### 3.4 关卡/回合系统（Stage System）
- **关卡编号**: 从 1 开始递增，显示为 `STAGE-{n}`
- **每轮流程**:
  1. 玩家瞄准并发射所有球
  2. 球运动、碰撞、反弹
  3. 所有球落回底部
  4. 计算消除得分
  5. 砖块下移一行
  6. 新砖块生成在顶行
  7. 更新发射点位置（第一个落回的球的 x 坐标）
  8. 进入下一轮瞄准阶段
- **Line 计数**: 显示当前砖块已下移到第几行（距离底部）
- **星级评价**: 根据消除效率给予 0-2 星评价

### 3.5 游戏结束条件
- 任意砖块下移到游戏区域底部（发射线位置）时，游戏结束
- 显示游戏结束界面：总分、到达关卡、星级等

---

## 四、特殊技能系统（Skills）

### 4.1 闪电技能（Lightning Skill）
- **位置**: 底部左侧红色圆形按钮
- **图标**: 闪电 ⚡
- **数量显示**: `x {n}`，如 `x 4`
- **效果**: 激活后清除一整行砖块（或对一行砖块造成大量伤害）
- **获取方式**: 通关奖励、广告观看、特殊道具

### 4.2 多球技能（Multi-Ball Skill）
- **位置**: 底部右侧红色圆形按钮
- **图标**: 多球聚集图标
- **数量显示**: `x {n}`，如 `x 5`
- **效果**: 激活后本轮球数量翻倍（或额外获得大量球）
- **获取方式**: 通关奖励、广告观看、特殊道具

### 4.3 能量条（Energy Bar）
- **位置**: 底部中间
- **表现**: 长条形进度条，中间有球形滑块
- **充能方式**: 每次消除砖块积累能量
- **满能效果**: 触发特殊能力（如穿透球、火球等）

---

## 五、视觉风格与特效

### 5.1 整体风格
- **主题**: 霓虹灯 / 赛博朋克风格
- **背景**: 深色（深蓝到黑色渐变），带微弱星光或粒子
- **颜色体系**:
  - 主色调: 霓虹蓝（边框、UI）
  - 砖块色: 紫色系 + 红色系 + 橙色系
  - 道具色: 白色/浅紫色发光
  - 技能按钮: 红色发光
  - 文字: 白色

### 5.2 特效列表
1. **砖块消除特效**: 碎片飞散 + 发光闪烁
2. **球碰撞特效**: 碰撞点微小闪光
3. **球轨迹特效**: 运动方向上留下短暂的虚线点
4. **发射特效**: 发射瞬间的光芒扩散
5. **砖块下移动画**: 平滑过渡动画
6. **新砖块生成动画**: 从上方淡入/滑入
7. **霓虹发光**: 砖块边框、游戏区域边框持续的 glow 效果
8. **UI按钮发光**: 技能按钮外圈发光环
9. **星级评价动画**: 星星点亮动画
10. **数字跳动**: 砖块被击中时数字跳动/闪烁

### 5.3 动画帧率
- 目标帧率: 60fps
- 使用 `requestAnimationFrame` 驱动游戏循环

---

## 六、数据结构设计

### 6.1 DataBus（全局状态管理 - 单例）
```javascript
class DataBus {
  // 游戏状态
  stage = 1;              // 当前关卡
  score = 0;              // 当前分数
  line = 0;               // 当前行数（砖块下移次数）
  isGameOver = false;     // 游戏是否结束
  isPaused = false;       // 是否暂停
  gameState = 'aiming';   // 'aiming' | 'launching' | 'running' | 'settling' | 'over'
  
  // 球相关
  balls = [];             // 所有球的数组
  ballCount = 1;          // 当前球数量
  nextBallCount = 0;      // 下一轮新增球数
  launchX = screenWidth / 2; // 发射点 X 坐标
  launchY = gameAreaBottom;   // 发射点 Y 坐标
  launchAngle = 0;        // 发射角度
  
  // 砖块相关
  bricks = [];            // 所有砖块二维数组 [row][col]
  
  // 道具相关
  pickups = [];           // 加球道具数组
  
  // 技能相关
  lightningCount = 0;     // 闪电技能次数
  multiBallCount = 0;     // 多球技能次数
  energy = 0;             // 能量条当前值
  maxEnergy = 100;        // 能量条最大值
  
  // 动画与特效
  animations = [];        // 活跃动画列表
  particles = [];         // 粒子特效列表
  
  // 评价
  stars = 0;              // 当前星级 (0-2)
  starProgress = 0;       // 星级进度条 (0-1)
  
  // 对象池
  pool = new Pool();
}
```

### 6.2 Ball（球）
```javascript
class Ball {
  x = 0;           // 当前X坐标
  y = 0;           // 当前Y坐标
  radius = 4;      // 球半径
  vx = 0;          // X方向速度
  vy = 0;          // Y方向速度
  speed = 8;       // 球速（像素/帧）
  active = true;   // 是否激活（运动中）
  trail = [];      // 轨迹点数组（最近N个位置）
}
```

### 6.3 Brick（砖块）
```javascript
class Brick {
  row = 0;          // 行索引
  col = 0;          // 列索引
  x = 0;            // 像素X坐标
  y = 0;            // 像素Y坐标
  width = 0;        // 砖块宽度
  height = 0;       // 砖块高度
  hp = 1;           // 当前生命值（数值）
  maxHp = 1;        // 初始生命值
  type = 'normal';  // 'normal' | 'triangle' | 'pickup'
  triangleDir = ''; // 三角砖块方向 'topLeft'|'topRight'|'bottomLeft'|'bottomRight'
  color = '';       // 当前颜色
  glowColor = '';   // 发光颜色
  isAlive = true;   // 是否存活
  shakeTimer = 0;   // 被击中抖动计时器
}
```

### 6.4 Pickup（加球道具）
```javascript
class Pickup {
  x = 0;
  y = 0;
  radius = 12;
  collected = false;
  type = 'ball';    // 'ball' | 'lightning' | 'multiball'
  glowPhase = 0;    // 发光动画相位
}
```

---

## 七、核心系统模块

### 7.1 文件结构规划
```
js/
├── main.js              # 游戏主循环、状态机
├── databus.js           # 全局数据总线
├── render.js            # Canvas 初始化
├── config.js            # 游戏配置常量（网格尺寸、颜色、速度等）
├── base/
│   ├── sprite.js        # 精灵基类
│   ├── pool.js          # 对象池
│   └── animation.js     # 动画基类
├── core/
│   ├── ball.js          # 球类（运动、碰撞检测、轨迹）
│   ├── brick.js         # 砖块类（数值、颜色、消除）
│   ├── pickup.js        # 道具类（加球等）
│   ├── grid.js          # 网格管理（7列布局、砖块生成、行下移）
│   ├── launcher.js      # 发射器（瞄准、角度计算、依次发射）
│   ├── collision.js     # 碰撞检测系统（球-砖块、球-墙壁、球-道具）
│   └── physics.js       # 物理计算（反弹角度、三角砖块法线）
├── effects/
│   ├── particle.js      # 粒子系统
│   ├── glow.js          # 霓虹发光渲染
│   ├── trail.js         # 球轨迹效果
│   └── shake.js         # 砖块受击抖动
├── runtime/
│   ├── background.js    # 背景渲染（深色渐变+粒子）
│   ├── hud.js           # 顶部HUD（关卡、分数、星级进度条）
│   ├── bottombar.js     # 底部操作栏（技能按钮、能量条）
│   ├── gameinfo.js      # 游戏信息（暂停、结束界面）
│   ├── music.js         # 音效管理
│   └── aimline.js       # 瞄准辅助虚线
├── level/
│   ├── levelgen.js      # 关卡生成器（砖块布局、数值计算）
│   └── difficulty.js    # 难度曲线（关卡递增规则）
└── libs/
    └── tinyemitter.js   # 事件系统
```

### 7.2 游戏主循环（Main Loop）
```
每帧执行:
1. 处理输入（touch事件）
2. 根据 gameState 更新逻辑:
   - 'aiming':   更新瞄准角度、绘制辅助线
   - 'launching': 依次发射球（每隔N帧发射一个）
   - 'running':  更新所有球位置、碰撞检测、砖块数值更新
   - 'settling': 所有球已落回，执行结算（下移砖块、生成新行、移动发射点）
   - 'over':     游戏结束界面
3. 渲染:
   - 背景
   - 游戏区域边框（霓虹发光）
   - 砖块（带发光、数字）
   - 道具
   - 球（带轨迹）
   - 辅助瞄准线（如启用）
   - 粒子特效
   - HUD（顶部信息栏）
   - 底部操作栏
4. requestAnimationFrame 继续下一帧
```

### 7.3 碰撞检测系统
- **球 vs 矩形砖块**: AABB 碰撞检测，判断碰撞面（上/下/左/右），据此反弹
- **球 vs 三角砖块**: 线段-圆碰撞检测，计算斜面法线进行反弹
- **球 vs 墙壁**: 简单边界检测
- **球 vs 底边**: 球停止运动，标记为非激活
- **球 vs 道具**: 圆-圆碰撞检测，拾取后道具消失
- **碰撞优化**: 仅检测球所在行列附近的砖块（空间分区优化）

### 7.4 关卡生成规则
- 每行 7 个格子，每轮在顶行生成新砖块
- 砖块填充率: 40%-70%（随关卡递增）
- 砖块数值 = stage × 基础系数（约1.0-1.5）
- 每行随机放置 1-3 个加球道具
- 偶尔出现三角砖块（增加变化性）
- 随着关卡推进：
  - 砖块数值增加
  - 填充率增加
  - 特殊砖块出现概率增加

---

## 八、触控交互设计

### 8.1 瞄准阶段
- `touchstart`: 记录起始触摸点
- `touchmove`: 计算触摸点与发射点连线的角度，实时更新辅助瞄准线
- `touchend`: 确定发射角度，切换到 launching 状态
- **瞄准线**: 从发射点出发的虚线，遇到墙壁/砖块时反弹继续延伸（最多显示2-3次反弹）

### 8.2 运行阶段
- 球运动中禁止触摸操作（或点击可加速）

### 8.3 技能使用
- 点击底部左侧闪电按钮: 消耗 1 次闪电，清除最底行砖块
- 点击底部右侧多球按钮: 消耗 1 次多球，下一轮球数翻倍

### 8.4 暂停
- 点击右上角暂停按钮: 暂停游戏，显示暂停菜单（继续/重新开始/退出）

---

## 九、音效设计

| 音效 | 文件 | 触发条件 |
|------|------|----------|
| 背景音乐 | bgm.mp3 | 游戏进行中循环播放 |
| 球碰砖块 | hit.mp3 | 球碰撞砖块时 |
| 砖块消除 | boom.mp3 | 砖块 HP 归零消除时 |
| 球发射 | bullet.mp3 | 每个球发射时 |
| 道具拾取 | pickup.mp3 | 球碰到加球道具时 |
| 技能激活 | skill.mp3 | 使用闪电/多球技能时 |
| 游戏结束 | gameover.mp3 | 游戏结束时 |
| 新关卡 | newstage.mp3 | 进入新关卡（新砖块行下来时） |

---

## 十、游戏配置常量（config.js）

```javascript
// 网格配置
const GRID_COLS = 7;               // 列数
const BRICK_PADDING = 2;           // 砖块间距
const GAME_AREA_TOP = 120;         // 游戏区域顶部Y坐标
const GAME_AREA_BOTTOM_OFFSET = 160; // 底部操作栏高度
const LAUNCH_Y_OFFSET = 60;        // 发射点距底部偏移

// 球配置
const BALL_RADIUS = 4;             // 球半径
const BALL_SPEED = 10;             // 球速（像素/帧）
const BALL_LAUNCH_INTERVAL = 3;    // 发射间隔（帧）
const BALL_TRAIL_LENGTH = 8;       // 轨迹长度

// 砖块配置
const BRICK_HP_MULTIPLIER = 1.0;   // HP = stage × multiplier
const BRICK_FILL_RATE_MIN = 0.4;   // 最小填充率
const BRICK_FILL_RATE_MAX = 0.7;   // 最大填充率
const PICKUP_PER_ROW_MIN = 1;      // 每行最少道具
const PICKUP_PER_ROW_MAX = 3;      // 每行最多道具

// 颜色配置（霓虹风格）
const COLORS = {
  background: '#0a0a2e',
  borderGlow: '#00d4ff',
  brickPurple: '#8b5cf6',
  brickRed: '#ef4444',
  brickOrange: '#f97316',
  brickGlow: 'rgba(139, 92, 246, 0.5)',
  ballColor: '#ffffff',
  ballGlow: 'rgba(255, 255, 255, 0.6)',
  pickupColor: '#e0e0ff',
  textColor: '#ffffff',
  skillButtonRed: '#dc2626',
  energyBar: '#00d4ff',
};

// 技能配置
const LIGHTNING_INITIAL = 4;
const MULTIBALL_INITIAL = 5;
const ENERGY_PER_BRICK = 5;
const MAX_ENERGY = 100;

// 星级评价
const STAR_1_THRESHOLD = 0.3;     // 消除30%砖块得1星
const STAR_2_THRESHOLD = 0.6;     // 消除60%砖块得2星
```

---

## 十一、开发优先级与里程碑

### Phase 1 - 核心玩法（MVP）
1. ✅ Canvas 初始化、全局状态管理（已有）
2. 🔲 网格系统 & 砖块渲染（7列矩形砖块，带数字）
3. 🔲 球的发射与直线运动
4. 🔲 球与墙壁碰撞反弹
5. 🔲 球与砖块碰撞检测 & 数值扣减
6. 🔲 砖块消除（HP归零时移除）
7. 🔲 所有球落回底部后结算（砖块下移、新行生成）
8. 🔲 游戏结束判定

### Phase 2 - 交互完善
9. 🔲 触摸瞄准系统（touchmove 角度计算）
10. 🔲 辅助瞄准虚线（含反弹预测）
11. 🔲 发射点随第一个落回球移动
12. 🔲 加球道具（碰到后下轮球+1）
13. 🔲 球的依次发射（间隔发射动画）

### Phase 3 - UI & 视觉
14. 🔲 霓虹风格背景（深色渐变）
15. 🔲 砖块霓虹发光边框
16. 🔲 砖块颜色系统（根据HP/maxHP比值变色）
17. 🔲 顶部HUD（关卡、行数、分数、星级进度条）
18. 🔲 底部操作栏（技能按钮、能量条）
19. 🔲 暂停功能 & 暂停菜单

### Phase 4 - 高级功能
20. 🔲 三角砖块（斜面反弹）
21. 🔲 闪电技能
22. 🔲 多球技能
23. 🔲 能量条系统
24. 🔲 粒子特效（消除、碰撞）
25. 🔲 球运动轨迹特效

### Phase 5 - 音效 & 打磨
26. 🔲 音效集成
27. 🔲 数字跳动动画（砖块被击中时）
28. 🔲 砖块下移平滑动画
29. 🔲 新砖块生成动画
30. 🔲 游戏结束界面 & 重新开始
31. 🔲 性能优化（对象池、空间分区碰撞）
32. 🔲 难度曲线微调

---

## 十二、关键技术要点

### 12.1 碰撞面判定算法
球碰砖块时需要判断碰撞面以决定反弹方向：
- 计算球心到砖块矩形四条边的距离
- 取最小穿透深度对应的边作为碰撞面
- 上/下碰撞：`vy = -vy`
- 左/右碰撞：`vx = -vx`
- 角碰撞（同时穿透两条边）：两个方向都反弹

### 12.2 瞄准线反弹预测
- 从发射点沿瞄准方向发射虚拟射线
- 检测射线与墙壁、砖块的交点
- 在交点处计算反弹方向
- 继续延伸射线（最多预测2-3次反弹）
- 渲染为虚线

### 12.3 球的依次发射
- 使用帧计数器控制发射间隔
- 每隔 `BALL_LAUNCH_INTERVAL` 帧从发射点创建一个新球
- 所有球共享相同的初始速度方向

### 12.4 性能优化
- 对象池复用 Ball 和 Particle 对象
- 砖块网格用二维数组索引，碰撞检测时只查找球附近的格子
- 粒子数量上限控制
- 离屏 Canvas 缓存不频繁变化的图层（如砖块层）

---

## 十三、注意事项

1. **微信小游戏 API**: 使用 `wx.createCanvas()` 创建画布，`wx.onTouchStart/Move/End` 处理触摸
2. **屏幕适配**: 通过 `wx.getWindowInfo()` 获取屏幕尺寸，所有坐标按比例计算
3. **竖屏模式**: `game.json` 中已设置 `"deviceOrientation": "portrait"`
4. **内存管理**: 大量球和粒子需要对象池管理，避免频繁 GC
5. **帧率稳定**: 游戏逻辑与渲染分离，确保在低端设备上也能流畅运行